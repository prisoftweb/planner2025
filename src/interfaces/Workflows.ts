export interface Workflow {
  _id: string
  title: string
  description: string
  status: boolean
  __v: number
  id: string
}

export interface WorkflowTable {
  id: string, 
  title: string,
  description: string
}