import { BudgetTableCostCenter } from "@/interfaces/Budget"

export default function TableCostCenter({dataTable}: {dataTable:BudgetTableCostCenter[]}) {
  return (
    <div>TableCostCenter</div>
  )
}
